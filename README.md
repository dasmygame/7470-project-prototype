# 7470-project-prototype
Proactive Task protoype for replication study of one click/swipe notification resolution

## Run Instructions

```bash
cd /7470-project-prototype
bash ./run.sh
```

If `bash` isn’t needed on your setup, you can also `chmod +x run.sh` and run `./run.sh`.
